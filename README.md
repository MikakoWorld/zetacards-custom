# zetacards-custom

Canvas ベースで再構築したカードメーカーです。

## 方針
- 背景テンプレ画像を使わない
- 出力は Canvas 直描画
- プレビューも同じ Canvas を縮小表示
- iPhone では PNG をページ内に表示し、長押し保存しやすくする

## 公開
GitHub Pages で `main` / root を公開元にするとそのまま動きます。

## テストページ
- 通常ページ: `index.html`
- 入力済みテストページ: `test.html`
